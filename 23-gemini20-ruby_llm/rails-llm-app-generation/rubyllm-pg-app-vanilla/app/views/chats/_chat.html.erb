<%# Existing content for displaying chat title/summary %>
<div id="<%= dom_id chat %>" class="w-full my-5 space-y-5">

  <div>
    <strong class="block font-big mb-1"><%= link_to chat.title, chat %></strong>
    ( <%= chat.messages.count rescue '?' %>📨
      by <%= chat.user.name rescue '-' %>)
    <em class="block font-medium mb-1"><%= chat.summary %></em>
    <tt class="block font-medium mb-1"><%= chat.model_id %></tt>
    <% if (chat.system_message rescue nil) %>
        ⚙️ <%= chat.system_message %>
    <% end %>

  </div>


</div>
